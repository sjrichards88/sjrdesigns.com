---
import logoAbba from '@/assets/images/logo-abba.svg'
import logoBarclays from '@/assets/images/logo-barclays.svg'
import logoBBC from '@/assets/images/logo-bbc.svg'
import logoDisney from '@/assets/images/logo-disney.svg'
import logoUbisoft from '@/assets/images/logo-ubisoft.svg'
---

<section id="about" class="section about">
  <div class="about__content">
    
    <!-- Section Header -->
    <div class="about__copy fade-in-element" style="animation-delay: 0.1s;">
      <h2>Senior Developer with Passion</h2>
      <p>
        <strong>8+ years of crafting exceptional digital experiences</strong> for world-renowned brands including Disney, BBC, and Barclays. I don't just write code—I solve business problems and create meaningful connections between brands and their audiences.
      </p>
      <p>
        My journey began with a simple fascination: <em>how can technology transform the way people interact with brands?</em> This question drives every project I work on, whether it's a high-traffic entertainment platform or an elegant corporate website.
      </p>
      <p>
        I specialize in <strong>modern web technologies</strong> including React, Next.js, and Astro, but my real strength lies in understanding your business goals and translating them into powerful digital solutions.
      </p>
    </div>

    <!-- Tech List -->
    <div class="tech-list fade-in-element" style="animation-delay: 0.3s;">
      <ul>
        <li><strong>Frontend:</strong> React, Next.js, Astro, TypeScript, JavaScript</li>
        <li><strong>Styling:</strong> Tailwind CSS, SCSS, CSS3, Responsive Design</li>
        <li><strong>Backend:</strong> Node.js, PHP, WordPress, Headless CMS</li>
        <li><strong>Tools:</strong> Git, Webpack, Vite, Performance Optimization</li>
        <li><strong>Focus:</strong> User Experience, Performance, Accessibility</li>
      </ul>
    </div>

    <!-- Company Logos -->
    <ul class="about__companies fade-in-element" style="animation-delay: 0.5s;">
      <li>
        <img src={logoBarclays.src} alt="Barclays" />
      </li>
      <li>
        <img src={logoBBC.src} alt="BBC" />
      </li>
      <li>
        <img src={logoAbba.src} alt="ABBA Voyage" />
      </li>
      <li>
        <img src={logoDisney.src} alt="Disney" />
      </li>
      <li>
        <img src={logoUbisoft.src} alt="Ubisoft" />
      </li>
    </ul>
  </div>
</section>

<script>
  // Smooth scroll for anchor links
  document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
      e.preventDefault();
      const target = document.querySelector(this.getAttribute('href'));
      if (target) {
        target.scrollIntoView({
          behavior: 'smooth',
          block: 'start'
        });
      }
    });
  });

  // Fixed animation system for about section
  document.addEventListener('DOMContentLoaded', function() {
    const animatedElements = document.querySelectorAll('.fade-in-element');
    
    // Use intersection observer for better performance
    const observer = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          const element = entry.target as HTMLElement;
          const delay = element.getAttribute('style')?.match(/animation-delay:\s*([\d.]+)s/);
          const animationDelay = delay ? parseFloat(delay[1]) * 1000 : 0;
          
          setTimeout(() => {
            element.classList.add('animate-in');
          }, animationDelay);
        }
      });
    }, { threshold: 0.1, rootMargin: '0px 0px -10% 0px' });

    // Observe all animated elements
    animatedElements.forEach(element => {
      observer.observe(element);
    });
  });
</script>

<style>
  /* Fixed animation flash - proper initial state */
  .fade-in-element {
    opacity: 0;
    transform: translateY(30px);
    transition: all 0.6s ease;
  }
  
  .fade-in-element.animate-in {
    opacity: 1;
    transform: translateY(0);
  }
</style>