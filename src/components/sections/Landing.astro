---
// Landing Section - Dark cinematic design with animated background gradients
import Button from '@/components/Button.astro';
---

<section 
  id="landing" 
  class="relative min-h-screen w-full bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900 overflow-hidden"
>
  <!-- Dot Pattern Background -->
  <div class="absolute inset-0 opacity-5" style="background-image: radial-gradient(circle at 2px 2px, white 1px, transparent 0); background-size: 40px 40px;"></div>
  
  <!-- Purple-Blue Gradient Orbs -->
  <div class="absolute inset-0">
    <!-- Primary purple orb -->
    <div class="absolute -top-32 -left-32 w-[600px] h-[600px] bg-gradient-to-br from-purple-500/20 to-blue-500/15 rounded-full blur-3xl will-change-transform animate-mega-float"></div>
    <!-- Secondary blue orb -->
    <div class="absolute -bottom-48 left-1/3 w-[700px] h-[700px] bg-gradient-to-br from-blue-500/20 to-indigo-500/15 rounded-full blur-3xl will-change-transform animate-mega-spiral"></div>
    <!-- Center accent -->
    <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-[800px] h-[800px] bg-gradient-to-br from-purple-500/10 to-blue-500/8 rounded-full blur-3xl will-change-transform animate-mega-pulse"></div>
  </div>
  
  <!-- Purple-Blue Geometric Elements -->
  <div class="absolute inset-0 md:opacity-20 opacity-10">
    <div class="absolute top-20 left-20 w-48 h-48 border border-purple-400/30 rounded-3xl rotate-45 will-change-transform animate-mega-spin"></div>
    <div class="absolute bottom-32 right-32 w-32 h-32 border border-blue-400/40 rounded-full will-change-transform animate-mega-bounce"></div>
    <div class="absolute top-1/3 right-20 w-24 h-24 bg-gradient-to-r from-purple-400/30 to-blue-400/25 rounded-2xl rotate-12 will-change-transform animate-mega-rotate"></div>
  </div>
  
  <!-- Purple-Blue particles -->
  <div class="absolute inset-0 md:opacity-30 opacity-15">
    <div class="absolute top-1/6 left-1/5 w-2 h-2 bg-purple-400 rounded-full will-change-transform animate-mega-twinkle"></div>
    <div class="absolute bottom-1/6 right-1/5 w-1 h-1 bg-blue-400 rounded-full will-change-transform animate-mega-twinkle-fast"></div>
  </div>
  
  <!-- Main Content Container with proper spacing from nav -->
  <div class="relative z-10 flex items-center justify-center min-h-screen px-4 sm:px-6 lg:px-8 py-24 sm:py-28">
    <div class="max-w-7xl mx-auto w-full">
      
        <!-- Status Badge with proper spacing from nav -->
        <div class="flex justify-center mb-8 sm:mb-10 fade-in-element fade-in-delay-200">
          <div class="relative inline-flex items-center gap-3 px-6 py-3 bg-emerald-500/20 border border-emerald-500/30 rounded-full backdrop-blur-sm shadow-lg">
            <div class="absolute inset-0 bg-gradient-to-r from-emerald-500/10 to-green-500/5 rounded-full blur-xl"></div>
            <div class="relative w-2.5 h-2.5 bg-emerald-400 rounded-full animate-pulse shadow-lg shadow-emerald-400/50"></div>
            <span class="relative text-emerald-400 text-sm font-medium">Available for Projects</span>
          </div>
        </div>

      <!-- Hero Content -->
      <div class="text-center space-y-4 sm:space-y-6">
        
        <!-- Main Heading with Glowing Keywords -->
        <div class="space-y-2 sm:space-y-3 fade-in-element fade-in-delay-400">
          <h1 class="font-heading font-bold leading-tight tracking-tight">
            <span class="block text-white drop-shadow-lg text-hero-name">
              Beautifully Crafted Websites
            </span>
            <span class="block bg-gradient-to-r from-purple-400 via-blue-400 to-indigo-400 bg-clip-text text-transparent mt-1 sm:mt-2 drop-shadow-lg glow-text text-hero-title">
              That Help Your Business Grow
            </span>
          </h1>
        </div>

        <!-- Subtitle with Glowing Keywords -->
        <div class="max-w-4xl mx-auto fade-in-element fade-in-delay-600">
          <p class="text-neutral-300 leading-relaxed font-light drop-shadow-sm text-hero-subtitle">
            Expert <span class="text-purple-400 font-medium glow-text-subtle">web development</span> and design that converts visitors into customers - fast, secure, and built to perform.
          </p>
        </div>

        <!-- CTA Buttons -->
        <div class="flex flex-col sm:flex-row gap-4 justify-center items-center pt-4 sm:pt-6 fade-in-element fade-in-delay-800">
          <Button href="#contact" variant="primary" size="lg">
            Start Your Project
          </Button>
          
          <Button href="#work" variant="outline" size="lg" icon="arrow">
            View Portfolio
          </Button>
        </div>

        <!-- Key Features Grid with Purple-Blue Gradient -->
        <div class="grid grid-cols-2 lg:grid-cols-4 gap-4 sm:gap-6 pt-8 sm:pt-12 max-w-4xl mx-auto fade-in-element fade-in-delay-1000">
          <div class="group text-center p-4 sm:p-6 rounded-2xl bg-white/5 border border-purple-400/20 backdrop-blur-sm hover:bg-purple-500/10 transition-all duration-300 hover:scale-105">
            <div class="absolute inset-0 bg-gradient-to-br from-purple-500/10 via-blue-500/10 to-cyan-500/5 rounded-2xl opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
            <div class="relative">
              <div class="text-xl sm:text-2xl mb-2">⚡</div>
              <h3 class="font-semibold text-white text-xs sm:text-sm">Lightning Fast</h3>
              <p class="text-neutral-400 text-xs mt-1">Optimized Performance</p>
            </div>
          </div>
          
          <div class="group text-center p-4 sm:p-6 rounded-2xl bg-white/5 border border-cyan-400/20 backdrop-blur-sm hover:bg-cyan-500/10 transition-all duration-300 hover:scale-105">
            <div class="absolute inset-0 bg-gradient-to-br from-cyan-500/10 to-teal-500/5 rounded-2xl opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
            <div class="relative">
              <div class="text-xl sm:text-2xl mb-2">🛡️</div>
              <h3 class="font-semibold text-white text-xs sm:text-sm">Secure & Reliable</h3>
              <p class="text-neutral-400 text-xs mt-1">Built with Best Practices</p>
            </div>
          </div>
          
          <div class="group text-center p-4 sm:p-6 rounded-2xl bg-white/5 border border-teal-400/20 backdrop-blur-sm hover:bg-teal-500/10 transition-all duration-300 hover:scale-105">
            <div class="absolute inset-0 bg-gradient-to-br from-teal-500/10 to-blue-500/5 rounded-2xl opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
            <div class="relative">
              <div class="text-xl sm:text-2xl mb-2">🚀</div>
              <h3 class="font-semibold text-white text-xs sm:text-sm">Scalable Solutions</h3>
              <p class="text-neutral-400 text-xs mt-1">Future-proof Architecture</p>
            </div>
          </div>
          
          <div class="group text-center p-4 sm:p-6 rounded-2xl bg-white/5 border border-neutral-400/20 backdrop-blur-sm hover:bg-neutral-500/10 transition-all duration-300 hover:scale-105">
            <div class="absolute inset-0 bg-gradient-to-br from-neutral-500/10 to-blue-500/5 rounded-2xl opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
            <div class="relative">
              <div class="text-xl sm:text-2xl mb-2">📱</div>
              <h3 class="font-semibold text-white text-xs sm:text-sm">Mobile First</h3>
              <p class="text-neutral-400 text-xs mt-1">Responsive Design</p>
            </div>
          </div>
        </div>

        <!-- Stats Row with Unified Styling -->
        <div class="flex flex-col sm:flex-row justify-center items-center gap-6 sm:gap-8 pt-8 sm:pt-12 pb-16 sm:pb-20 fade-in-element fade-in-delay-1200">
          <div class="text-center group">
            <div class="font-bold text-white drop-shadow-lg group-hover:text-purple-400 transition-colors duration-300 glow-text-subtle text-hero-stat">8+</div>
            <div class="text-neutral-400 text-sm">Years Experience</div>
          </div>
          <div class="hidden sm:block w-px h-8 sm:h-12 bg-gradient-to-b from-transparent via-white/20 to-transparent"></div>
          <div class="text-center group">
            <div class="font-bold text-white drop-shadow-lg group-hover:text-blue-400 transition-colors duration-300 glow-text-subtle text-hero-stat">50+</div>
            <div class="text-neutral-400 text-sm">Projects Delivered</div>
          </div>
          <div class="hidden sm:block w-px h-8 sm:h-12 bg-gradient-to-b from-transparent via-white/20 to-transparent"></div>
          <div class="text-center group">
            <div class="font-bold text-white drop-shadow-lg group-hover:text-indigo-400 transition-colors duration-300 glow-text-subtle text-hero-stat">100%</div>
            <div class="text-neutral-400 text-sm">Client Satisfaction</div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Enhanced Scroll Indicator -->
  <div class="absolute bottom-4 sm:bottom-8 left-0 right-0 z-50 fade-in-element fade-in-delay-1400 flex justify-center">
    <div class="flex flex-col items-center text-neutral-400 group hover:text-blue-400 transition-colors duration-300">
      <span class="text-xs uppercase tracking-wider mb-2">Scroll</span>
      <div class="w-px h-6 sm:h-8 bg-gradient-to-b from-white/30 to-transparent animate-bounce"></div>
    </div>
  </div>
</section>

<style>
  /* Hero responsive typography */
  .text-hero-name {
    font-size: clamp(2rem, 8vw, 4.5rem);
  }
  
  .text-hero-title {
    font-size: clamp(1.25rem, 5vw, 3.5rem);
  }
  
  .text-hero-subtitle {
    font-size: clamp(1rem, 2.5vw, 1.5rem);
  }
  
  .text-hero-stat {
    font-size: clamp(1.5rem, 4vw, 2.5rem);
  }

  /* Animation delays */
  .fade-in-delay-200 {
    animation-delay: 0.2s;
  }
  
  .fade-in-delay-400 {
    animation-delay: 0.4s;
  }
  
  .fade-in-delay-600 {
    animation-delay: 0.6s;
  }
  
  .fade-in-delay-800 {
    animation-delay: 0.8s;
  }
  
  .fade-in-delay-1000 {
    animation-delay: 1s;
  }
  
  .fade-in-delay-1200 {
    animation-delay: 1.2s;
  }
  
  .fade-in-delay-1400 {
    animation-delay: 1.4s;
  }

  /*  Performance-optimized glow effects */
  .glow-text {
    text-shadow: 0 0 20px rgba(59, 130, 246, 0.3), 0 0 40px rgba(34, 211, 238, 0.2);
  }

  .glow-text-subtle {
    text-shadow: 0 0 10px rgba(59, 130, 246, 0.2), 0 0 20px rgba(34, 211, 238, 0.1);
    transition: text-shadow 0.3s ease;
  }

  .glow-text-subtle:hover {
    text-shadow: 0 0 15px rgba(59, 130, 246, 0.4), 0 0 30px rgba(34, 211, 238, 0.2);
  }

  /* Optimized animations with GPU acceleration */
  @keyframes mega-float {
    0%, 100% {
      transform: translate3d(0px, 0px, 0) scale(1);
    }
    25% {
      transform: translate3d(50px, -80px, 0) scale(1.05);
    }
    50% {
      transform: translate3d(-25px, -40px, 0) scale(0.95);
    }
    75% {
      transform: translate3d(35px, -60px, 0) scale(1.02);
    }
  }

  @keyframes mega-spiral {
    0%, 100% {
      transform: translate3d(0px, 0px, 0);
    }
    25% {
      transform: translate3d(0px, -50px, 0);
    }
    50% {
      transform: translate3d(0px, -25px, 0);
    }
    75% {
      transform: translate3d(0px, -75px, 0);
    }
  }

  @keyframes mega-pulse {
    0%, 100% {
      transform: translate3d(-50%, -50%, 0) scale(1);
      opacity: 0.1;
    }
    50% {
      transform: translate3d(-50%, -50%, 0) scale(1.1);
      opacity: 0.2;
    }
  }

  @keyframes mega-spin {
    0%, 100% {
      transform: scale(1);
    }
    50% {
      transform: scale(1.05);
    }
  }

  @keyframes mega-bounce {
    0%, 100% {
      transform: translate3d(0px, 0px, 0) scale(1);
    }
    50% {
      transform: translate3d(0px, -20px, 0) scale(1.05);
    }
  }

  @keyframes mega-rotate {
    0%, 100% {
      transform: scale(1);
    }
    50% {
      transform: scale(1.08);
    }
  }

  @keyframes mega-twinkle {
    0%, 100% {
      opacity: 0.3;
      transform: scale(1);
    }
    50% {
      opacity: 1;
      transform: scale(1.3);
    }
  }

  @keyframes mega-twinkle-fast {
    0%, 100% {
      opacity: 0.2;
      transform: scale(0.8);
    }
    50% {
      opacity: 0.7;
      transform: scale(1.1);
    }
  }

  @keyframes fade-in-up {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Optimized animation classes with better performance */
  .animate-mega-float {
    animation: mega-float 25s ease-in-out infinite;
    will-change: transform;
  }

  .animate-mega-spiral {
    animation: mega-spiral 35s linear infinite;
    will-change: transform;
  }

  .animate-mega-pulse {
    animation: mega-pulse 20s ease-in-out infinite;
    will-change: transform, opacity;
  }

  .animate-mega-spin {
    animation: mega-spin 30s ease-in-out infinite;
    will-change: transform;
  }

  .animate-mega-bounce {
    animation: mega-bounce 8s ease-in-out infinite;
    will-change: transform;
  }

  .animate-mega-rotate {
    animation: mega-rotate 35s ease-in-out infinite;
    will-change: transform;
  }

  .animate-mega-twinkle {
    animation: mega-twinkle 4s ease-in-out infinite;
    will-change: opacity, transform;
  }

  .animate-mega-twinkle-fast {
    animation: mega-twinkle-fast 3s ease-in-out infinite;
    will-change: opacity, transform;
  }

  .animate-fade-in-up {
    animation: fade-in-up 1s ease-out forwards;
  }
  
  /* Fixed animation flash - proper initial state */
  .fade-in-element {
    opacity: 0;
    transform: translateY(30px);
    transition: all 0.6s ease;
  }
  
  .fade-in-element.animate-in {
    opacity: 1;
    transform: translateY(0);
  }

  /* Mobile optimizations */
  @media (max-width: 768px) {
    /* Slower animations on mobile for better performance */
    .animate-mega-float {
      animation-duration: 45s;
    }
    
    .animate-mega-spiral {
      animation-duration: 60s;
    }
    
    .animate-mega-pulse {
      animation-duration: 35s;
    }
    
    .animate-mega-spin {
      animation-duration: 50s;
    }
    
    .animate-mega-bounce {
      animation-duration: 15s;
    }
    
    .animate-mega-rotate {
      animation-duration: 60s;
    }
    
    .animate-mega-twinkle {
      animation-duration: 6s;
    }
    
    .animate-mega-twinkle-fast {
      animation-duration: 5s;
    }
  }

  /* Reduce motion for users who prefer it */
  @media (prefers-reduced-motion: reduce) {
    .animate-mega-float,
    .animate-mega-spiral,
    .animate-mega-pulse,
    .animate-mega-spin,
    .animate-mega-bounce,
    .animate-mega-rotate,
    .animate-mega-twinkle,
    .animate-mega-twinkle-fast {
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
      opacity: 0.5;
    }
  }
</style>

<script>
  // Performance-optimized animations
  document.addEventListener('DOMContentLoaded', function() {
    // Optimized fade-in animation system
    const animatedElements = document.querySelectorAll('.fade-in-element');
    
    // Use RAF for better performance
    const animateElements = () => {
      animatedElements.forEach((element, index) => {
        const delay = element.getAttribute('style')?.match(/animation-delay:\s*([\d.]+)s/);
        const animationDelay = delay ? parseFloat(delay[1]) * 1000 : index * 200;
        
        setTimeout(() => {
          requestAnimationFrame(() => {
            (element as HTMLElement).classList.add('animate-in');
          });
        }, animationDelay);
      });
    };

    // Start animations
    animateElements();

    // Optimized smooth scroll for anchor links
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
      anchor.addEventListener('click', function (e) {
        e.preventDefault();
        const target = document.querySelector(this.getAttribute('href'));
        if (target) {
          target.scrollIntoView({
            behavior: 'smooth',
            block: 'start'
          });
        }
      });
    });

    // Passive scroll listener for better performance
    let ticking = false;
    let lastScrollY = 0;
    
    const handleScroll = () => {
      lastScrollY = window.pageYOffset;
      
      if (!ticking) {
        requestAnimationFrame(() => {
          // Only apply parallax effect if user hasn't scrolled past the landing section
          if (lastScrollY < window.innerHeight) {
            const rate = lastScrollY * -0.05; // Reduced parallax intensity
            const orbs = document.querySelectorAll('#landing .animate-mega-float, #landing .animate-mega-spiral');
            
            orbs.forEach((orb, index) => {
              const element = orb as HTMLElement;
              const speed = (index + 1) * 0.02; // Reduced parallax speed
              element.style.transform = `translate3d(0, ${rate * speed}px, 0)`;
            });
          }
          
          ticking = false;
        });
        ticking = true;
      }
    };

    // Use passive listener for better scroll performance
    window.addEventListener('scroll', handleScroll, { passive: true });

    // Cleanup on page unload
    window.addEventListener('beforeunload', () => {
      window.removeEventListener('scroll', handleScroll);
    });
  });
</script>